fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Keep installs clean when teammates already have LangChain/LangSmith in the
# same environment. The current MVP does not call LangChain yet, but these
# packages satisfy dependency checks commonly triggered by preinstalled AI libs.
jsonpatch>=1.33,<2.0
zstandard>=0.23.0,<0.24.0
